import {Navigate} from 'react-router-dom';import {useAuth} from '../../context/AuthContext';import {canonicalRole} from '../../utils/role';export default function ProtectedRoute({roles,children}){const{user}=useAuth();if(!user)return <Navigate to="/login" replace/>;const role=canonicalRole(user.role);if(roles&&!roles.includes(role)){const fallback=['user','agent','marketing'].includes(role)?'/app':['operator','analis','master','admin'].includes(role)?'/credit-applications':'/login';return <Navigate to={fallback} replace/>}return children}
