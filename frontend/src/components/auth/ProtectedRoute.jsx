import {Navigate} from 'react-router-dom';import {useAuth} from '../../context/AuthContext';export default function ProtectedRoute({roles,children}){const{user}=useAuth();if(!user)return <Navigate to="/login" replace/>;if(roles&&!roles.includes(user.role)){const fallback=['user','agent','marketing'].includes(user.role)?'/app':['operator','analis'].includes(user.role)?'/credit-applications':'/dashboard';return <Navigate to={fallback} replace/>}return children}
