import {useMemo,useState} from 'react';import {useNavigate} from 'react-router-dom';import {Smartphone,ArrowRight,Wifi} from 'lucide-react';import {detectOperator} from '../../constants/operators';import OperatorBadge from './OperatorBadge';export default function QuickTopUp(){const[phone,setPhone]=useState(''),navigate=useNavigate(),operator=useMemo(()=>detectOperator(phone),[phone]);return <section className="quick-topup"><div className="quick-copy"><span>TRANSAKSI CEPAT</span><h2>Isi pulsa & paket data<br/>dalam hitungan detik.</h2><p>Semua operator Indonesia tersedia, harga agen lebih hemat, dan transaksi diproses otomatis 24 jam.</p><div><b><Wifi/>Online 24/7</b><b><Smartphone/>5+ Operator</b></div></div><div className="quick-form"><label>Nomor tujuan</label><div className="phone-field"><span>+62</span><input value={phone} onChange={e=>setPhone(e.target.value.replace(/\D/g,'').slice(0,13))} placeholder="812 3456 7890"/></div><OperatorBadge operator={operator}/><button onClick={()=>navigate('/topup',{state:{phone}})} disabled={phone.length<9}>Lanjut Pilih Produk <ArrowRight size={17}/></button></div></section>}
