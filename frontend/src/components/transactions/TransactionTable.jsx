import Avatar from '../common/Avatar';import StatusBadge from '../common/StatusBadge';import {rupiah} from '../../utils/currency';import {relativeTime} from '../../utils/date';export default function TransactionTable({items=[]}){return <div className="table-scroll"><table><thead><tr><th>ID Transaksi</th><th>Pelanggan</th><th>Metode</th><th>Tanggal</th><th>Jumlah</th><th>Status</th></tr></thead><tbody>{items.map(t=><tr key={t.id}><td><strong>{t.id}</strong></td><td><div className="customer-cell"><Avatar name={t.customer}/><div><strong>{t.customer}</strong><small>{t.email}</small></div></div></td><td>{t.method}</td><td>{relativeTime(t.created_at)}</td><td><strong>{rupiah(t.amount)}</strong></td><td><StatusBadge status={t.status}/></td></tr>)}{!items.length&&<tr><td colSpan="6" className="empty-row">Tidak ada transaksi ditemukan.</td></tr>}</tbody></table></div>}
