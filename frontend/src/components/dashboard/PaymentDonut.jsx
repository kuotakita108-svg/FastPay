export default function PaymentDonut({methods=[]}){const rows=Array.isArray(methods)?methods:[],colors=['#6c5ce7','#20b486','#f5a348','#4d91e8'];const parts=rows.map((m,i)=>{const start=rows.slice(0,i).reduce((sum,item)=>sum+item.share,0);return `${colors[i]} ${start}% ${start+m.share}%`});return <section className="panel donut-panel"><div className="panel-header"><div><h2>Metode Pembayaran</h2><p>Distribusi transaksi</p></div></div><div className="donut" style={{background:`conic-gradient(${parts.join(',')})`}}><div><strong>1.248</strong><small>Transaksi</small></div></div><div className="legend">{rows.map((m,i)=><div key={m.name}><i style={{background:colors[i]}}/><span>{m.name}</span><strong>{m.share}%</strong></div>)}</div></section>}
