import {shortRupiah} from '../../utils/currency';export default function RevenueChart({data=[]}){const max=Math.max(...data.map(x=>x.revenue),1);return <section className="panel chart-panel"><div className="panel-header"><div><h2>Pendapatan</h2><p>Performa pendapatan 7 hari terakhir</p></div><select><option>7 hari terakhir</option><option>30 hari terakhir</option></select></div><div className="bars">{data.map(point=><div className="bar-item" key={point.label}><div className="bar" title={shortRupiah(point.revenue)} style={{height:`${point.revenue/max*88}%`}}/><span>{point.label}</span></div>)}</div></section>}
