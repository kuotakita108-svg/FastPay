import {ArrowLeft,Bell} from 'lucide-react';import {useNavigate} from 'react-router-dom';export default function SubPageHeader({title,description,back=false}){const navigate=useNavigate();return <header className="subpage-header">{back&&<button onClick={()=>navigate(-1)}><ArrowLeft/></button>}<div><strong>{title}</strong><small>{description}</small></div><button><Bell/><i/></button></header>}
