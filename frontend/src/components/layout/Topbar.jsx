import {Menu,Search,Moon,Sun,Bell,Plus} from 'lucide-react';import {useNavigate} from 'react-router-dom';import {useState} from 'react';import {useTheme} from '../../context/ThemeContext';export default function Topbar({onMenu}){const{dark,toggle}=useTheme(),navigate=useNavigate(),[query,setQuery]=useState('');const submit=e=>{e.preventDefault();if(query)navigate(`/transactions?q=${encodeURIComponent(query)}`)};return <header className="topbar"><button className="menu-button" onClick={onMenu}><Menu/></button><form className="global-search" onSubmit={submit}><Search size={18}/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Cari produk, nomor, transaksi..."/><kbd>⌘ K</kbd></form><div className="top-actions"><button className="icon-button" onClick={toggle}>{dark?<Sun size={18}/>:<Moon size={18}/>}</button><button className="icon-button notification"><Bell size={18}/><i/></button><button className="primary-button" onClick={()=>navigate('/topup')}><Plus size={17}/> Transaksi Pulsa</button></div></header>}
