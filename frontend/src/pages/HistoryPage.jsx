import {useMemo,useState} from 'react';import {Search,CalendarDays,ChevronRight,ReceiptText} from 'lucide-react';import SubPageHeader from '../components/mobile/SubPageHeader';import StatusBadge from '../components/common/StatusBadge';import LoadingState from '../components/common/LoadingState';import MobileNav from '../components/mobile/MobileNav';import {useAsync} from '../hooks/useAsync';import {getTransactions} from '../services/transactionService';import {rupiah} from '../utils/currency';import {formatDate} from '../utils/date';
export default function HistoryPage(){const{data=[],loading}=useAsync(getTransactions),[filter,setFilter]=useState('all'),[query,setQuery]=useState(''),[date,setDate]=useState('');const items=useMemo(()=>{const now=new Date(),today=new Date(now.getFullYear(),now.getMonth(),now.getDate());return data?.filter(item=>{const created=new Date(item.created_at),matchesText=item.customer.toLowerCase().includes(query.toLowerCase())||item.id.toLowerCase().includes(query.toLowerCase()),matchesDate=!date||item.created_at.startsWith(date);let matchesPeriod=true;if(filter==='today')matchesPeriod=created>=today;if(filter==='yesterday'){const yesterday=new Date(today);yesterday.setDate(today.getDate()-1);matchesPeriod=created>=yesterday&&created<today}if(filter==='week'){const week=new Date(today);week.setDate(today.getDate()-7);matchesPeriod=created>=week}return matchesText&&matchesDate&&matchesPeriod})||[]},[data,query,date,filter]);return <main className="mobile-app history-page"><SubPageHeader title="Riwayat Transaksi" description="Pantau semua transaksi PulsaPrime"/><section className="history-tools"><div className="history-search"><Search/><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Cari nomor atau ID transaksi"/></div><div className="period-filters">{[['all','Semua'],['today','Hari ini'],['yesterday','Kemarin'],['week','7 Hari']].map(([key,label])=><button className={filter===key?'active':''} onClick={()=>setFilter(key)} key={key}>{label}</button>)}</div><label className="date-picker"><CalendarDays/>Pilih tanggal<input type="date" value={date} onChange={e=>setDate(e.target.value)}/></label></section>{loading?<LoadingState cards={3}/>:<section className="history-list">{items.map(item=><article key={item.id}><i><ReceiptText/></i><div><strong>{item.customer}</strong><small>{item.method}</small><span>{formatDate(item.created_at)} · {item.id}</span></div><aside><b>{rupiah(item.amount)}</b><StatusBadge status={item.status}/></aside><ChevronRight/></article>)}{!items.length&&<div className="history-empty"><ReceiptText/><strong>Belum ada transaksi</strong><p>Transaksi pada tanggal yang dipilih tidak ditemukan.</p></div>}</section>}<MobileNav/></main>}
