import {useState} from 'react'
import {Navigate,useLocation,useNavigate} from 'react-router-dom'
import {ArrowLeft,Check,ChevronRight,Clock3,ReceiptText,ShieldCheck,WalletCards,XCircle} from 'lucide-react'
import {useAuth} from '../context/AuthContext'
import {createTransaction,payWithBalance} from '../services/transactionService'
import {rupiah} from '../utils/currency'
export default function CheckoutPage(){const{state}=useLocation(),navigate=useNavigate(),{session,user,setBalance,deductBalance}=useAuth(),[processing,setProcessing]=useState(false),[result,setResult]=useState(null),[error,setError]=useState('');if(!state?.amount)return <Navigate to="/app/services" replace/>;const pay=async()=>{setProcessing(true);setError('');try{let response;if(session?.offline){deductBalance(state.amount);const transaction=await createTransaction({customer:state.target,email:user.email||`${user.id}@fastpay.local`,method:`${state.provider} · ${state.title}`,amount:state.amount});response={transaction,balance:Number(user.balance)-Number(state.amount)}}else{response=await payWithBalance({...state,email:user.email});setBalance(response.balance)}setResult(response)}catch(current){setError(current.message)}finally{setProcessing(false)}};if(result)return <main className="mobile-app checkout-page"><section className="payment-result"><i><Check/></i><span>PEMBAYARAN BERHASIL</span><h1>{rupiah(state.amount)}</h1><p>{state.product} untuk {state.target} berhasil diproses.</p><div><small>ID Transaksi</small><strong>{result.transaction.id}</strong></div><button onClick={()=>navigate('/app/history')}>Lihat Riwayat</button><button className="ghost" onClick={()=>navigate('/app')}>Kembali ke Beranda</button></section></main>;return <main className="mobile-app checkout-page"><header className="checkout-head"><button onClick={()=>navigate(-1)}><ArrowLeft/></button><div><strong>Konfirmasi Pembayaran</strong><small>Periksa kembali sebelum membayar</small></div><i><ShieldCheck/></i></header><section className="checkout-status"><Clock3/>Pesananmu siap dibayar</section><section className="checkout-card order"><header><ReceiptText/><div><small>Produk</small><strong>{state.product}</strong></div></header><dl><div><dt>Layanan</dt><dd>{state.title}</dd></div><div><dt>Penyedia</dt><dd>{state.provider}</dd></div><div><dt>Tujuan</dt><dd>{state.target}</dd></div></dl></section><section className="checkout-card"><h2>Metode Pembayaran</h2><button className="payment-method selected"><i><WalletCards/></i><div><strong>Saldo FastPay</strong><small>Saldo tersedia {rupiah(user.balance)}</small></div><Check/></button><button className="payment-method disabled" disabled><i><ShieldCheck/></i><div><strong>QRIS & Virtual Account</strong><small>Aktif setelah gateway pembayaran terhubung</small></div><ChevronRight/></button></section><section className="checkout-card summary"><h2>Rincian Pembayaran</h2><div><span>Harga produk</span><b>{rupiah(state.amount)}</b></div><div><span>Biaya layanan</span><b>Gratis</b></div><div className="total"><span>Total pembayaran</span><strong>{rupiah(state.amount)}</strong></div></section>{error&&<div className="checkout-error"><XCircle/><span>{error}</span>{/saldo/i.test(error)&&<button onClick={()=>navigate('/app/balance/topup')}>Isi Saldo</button>}</div>}<footer className="checkout-footer"><div><span>Total</span><strong>{rupiah(state.amount)}</strong></div><button disabled={processing} onClick={pay}>{processing?'Memproses...':'Bayar Sekarang'}</button></footer></main>}
