import {createContext,useContext,useState} from 'react';import {login as loginRequest,register as registerRequest} from '../services/authService';const AuthContext=createContext();export function AuthProvider({children}){const[session,setSession]=useState(()=>{try{return JSON.parse(localStorage.getItem('fastpay_session'))}catch{return null}});const save=result=>{localStorage.setItem('fastpay_session',JSON.stringify(result));setSession(result);return result};const login=async credentials=>save(await loginRequest(credentials));const register=async profile=>save(await registerRequest(profile));const addBalance=amount=>{const next={...session,user:{...session.user,balance:Number(session.user.balance||0)+Number(amount)}};save(next);return next.user.balance};const logout=()=>{localStorage.removeItem('fastpay_session');setSession(null)};return <AuthContext.Provider value={{session,user:session?.user,login,register,addBalance,logout}}>{children}</AuthContext.Provider>}export const useAuth=()=>useContext(AuthContext);
