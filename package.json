{
  "name": "kuotakita-cloudflare",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build",
    "deploy": "npm run build && wrangler pages deploy frontend/dist --project-name kuotakita-app --branch main"
  },
  "devDependencies": {"wrangler": "^4.45.0"}
}
